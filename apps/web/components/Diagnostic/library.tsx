import React, { FC } from 'react'

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface LibraryProps {}

const Library: FC = (props: LibraryProps) => {
  return <div></div>
}

export default Library
